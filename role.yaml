apiVersion: rbac.authorization.k8s.io/v1

kind: Role

metadata:
  name: mustafa-role
  namespace: dev


rules:

- apiGroups:
  - ""

  resources:
  - pods
  - deployments

  verbs:
  - get
  - watch
  - list
  - create
  - delete
